# Used by `npm run dev` and `vite build --mode development`. Every backend service in local
# docker-compose still publishes its port back to the host, so the browser always reaches the
# gateway at localhost regardless of whether the services behind it run natively or in containers.
VITE_API_BASE_URL=http://localhost:8085/api/auth
