# Baked into the build at CI/CD time (Vite env vars are compile-time, not runtime) - replace with
# the real public gateway domain for the production deployment before building.
VITE_API_BASE_URL=https://api.berkay.com/api/auth
