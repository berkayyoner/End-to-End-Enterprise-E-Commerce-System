spring:
  application:
    name: order-service
  profiles:
    default: local
  config:
    import: "optional:configserver:${CONFIG_SERVER_URI:http://127.0.0.1:8888}"
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always

server:
  port: 8084

logging:
  level:
    root: INFO
    com.berkay: DEBUG
