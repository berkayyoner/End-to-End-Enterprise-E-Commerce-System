spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: berkay
    password: "1234"
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

berkay:
  oauth2:
    issuer-uri: http://localhost:8080/api/auth
    public-client:
      redirect-uris:
        - http://localhost:5173/oauth2/callback
        - http://localhost:4173/oauth2/callback
    personnel-client:
      redirect-uris:
        - http://localhost:5174/oauth2/callback
        - http://localhost:4174/oauth2/callback
  personnel:
    bootstrap:
      email: admin@berkay.local
      password: ChangeMe123!

logging:
  level:
    org.hibernate.SQL: DEBUG
