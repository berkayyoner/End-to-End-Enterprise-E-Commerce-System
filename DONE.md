Project Start Date: August 16, 2026
0.1: Documented repository folder/module conventions, service naming pattern, and database table ownership boundaries in CONVENTIONS.md.
0.2: Added docker-compose.yml orchestrating Oracle XE (XEPDB1/berkay/1521), Redis, Elasticsearch, and Dockerfiles for api/auth-service/product-service/berkay-public/berkay-personnel, with env-overridable datasource config wired into each service; validated via `docker compose config`.
0.3: Replaced application.properties with application.yml + application-local.yml/application-development.yml/application-production.yml per service (api, auth-service, product-service), local using literal localhost Oracle/Redis/Elasticsearch, development using docker-compose service hostnames, production requiring env vars with no defaults; verified all three services boot successfully against the local Oracle instance with the `local` profile and docker-compose config still validates.
0.4: Added common-lib module with BaseEntity (IDENTITY id, created/updated at+by via Spring Data auditing, is_deleted/deleted_at soft-delete with softDelete()/restore()) and a JpaAuditingAutoConfiguration auto-registered via AutoConfiguration.imports; wired it as a dependency into api/auth-service/product-service, added a root aggregator pom.xml, updated their Dockerfiles + docker-compose.yml to build common-lib from repo-root context first; verified common-lib installs, all three services compile against it, and auth-service still boots cleanly against the local Oracle instance.
0.5: Designed the backend multi-language strategy in I18N.md (default locale tr, per-service-owned language/translation reference data, product_translation-style pattern) and added common-lib's LocaleCodes, TranslationEntity base class, and TranslationResolver (exact locale -> default tr -> any available) with a passing TranslationResolverTest; reinstalled common-lib and reconfirmed api/auth-service/product-service compile against it.
0.6: Adapted example-bases/hooks+locales into a per-app src/i18n module (config, tr.json/en.json, translate.js with tr-default fallback, context.js + LanguageContext provider + useTranslation hook) for both berkay-public and berkay-personnel, wired via I18nProvider in main.jsx, and replaced the Vite boilerplate App.jsx with a minimal translated home screen plus a language switcher; verified with clean `npm run lint` and `npm run build` on both apps.