spring:
  application:
    name: log-service
  profiles:
    default: local
  config:
    import: "optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}"

server:
  port: 8083

logging:
  level:
    root: INFO
    com.berkay: DEBUG
