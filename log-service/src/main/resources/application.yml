spring:
  application:
    name: log-service
  profiles:
    default: local
  config:
    import: "optional:configserver:${CONFIG_SERVER_URI:http://127.0.0.1:8888}"

server:
  port: 8083

logging:
  level:
    root: INFO
    com.berkay: DEBUG
